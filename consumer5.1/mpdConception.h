#include <iostream>

using namespace std;

namespace svcdash{
	class mpdConception{
		public:
		    string id;
		    int width;
		    int height;
		    string codec;
		    int bandwidth;
		    int segmentDuration;
		    string frameRate;
		    int totalSegments;
	};
}
